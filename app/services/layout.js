export const metadata = {
  title: "Services",
  description: "This is a service page of HR Sohel's website",
};

export default function RootLayout({ children }) {
  return <section>{children}</section>;
}
