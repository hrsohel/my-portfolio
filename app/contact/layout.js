export const metadata = {
  title: "Contact",
  description: "This is a contact page of HR Sohel's website",
};

export default function RootLayout({ children }) {
  return <section>{children}</section>;
}
