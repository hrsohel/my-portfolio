export const metadata = {
  title: "Login",
  description: "This is a login page of HR Sohel's website",
};

export default function RootLayout({ children }) {
  return <section>{children}</section>;
}
